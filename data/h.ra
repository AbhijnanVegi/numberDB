LOAD Student
LOAD Marks
C <- CROSS Student, Marks
FT <- SELECT Student_Stud_Id == Marks_Stud_Id FROM C
A1 <- SELECT Advisor_id == 2 FROM FT
MTH <- SELECT Maths_marks > 90 FROM A2
T <- PROJECT Student_Stud_Id FROM MTH
RENAME Student_Stud_Id TO Stud_Id FROM T
PRINT T

